1. 格式规范：使用纯文本格式，禁用 Markdown 列表符号（-、*、**）。
2. 层级规范：使用 # 作为一级标题。子标题或补充信息使用二级（##）或三级（###）标题。严禁复用一级标题。优先使用数字罗列清晰的内容。
3. 间距规范：一级标题之前必须留空行隔开，但一级与二级、二级与二级等标题之间不留空行，保持紧凑。
4. 内容规范：严禁使用模糊的内部术语或“黑话”，必须使用通用的工程化语言进行技术描述。
5. 记录要求：禁止主动更新本文件中的知识点内容，仅在用户明确要求时更新。所有新增的 AI 行为规则或格式要求，必须同步更新到本栏。
6. 简历产出：简历重构必须以“功能实现”为绝对核心，详细展示底层硬核技术的实现过程、逻辑链条及解决问题的含金量。
7. 格式初衷：此格式通过强制去装饰化（禁用加粗、点状列表）和结构化编号，确保文档在嵌入式等硬核技术领域的严谨性与工业质感。通过标题间距的极致压缩，使复杂技术逻辑在有限空间内呈现出高度紧凑的结构化特征。
8. 变更限制：在执行格式修正时，严禁擅自修改、添加或删除原始内容（如文件路径引用、代码逻辑描述等）。AI 仅被授权调整排版格式（如层级、间距、编号方式），必须确保原文信息 100% 完整保留。
9. 格式独立性：即使参考其他源文件内容，也必须强制转换并遵循本文件的“纯文本+数字编号”格式规范。严禁引入 LaTeX 公式、点状列表或加粗等装饰。同时必须确保多层级编号逻辑统一（如 1. -> 1.1 或缩进编号），严禁出现序号错乱。
10. 内容保护规则：严禁删除用户手动编写的总结、想法、理解或原创比喻，除非内容完全重复或用户明确要求修改。AI 必须优先保护用户个人见解在文档中的完整性与原创性。
11. 视觉精简准则：正文内容中严禁使用 ** 加粗、* 斜体 或 LaTeX 公式进行装饰。所有技术术语、参数值、尺寸信息必须以纯文本形式呈现，确保文档整体视觉风格的高度统一与极简主义。
