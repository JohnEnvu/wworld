13. 命令行格式化规范：所有可执行的命令行必须单独包裹在 ```bash 代码块中，方便直接点击复制。代码块内注释必须保持极度精简（仅限核心物理动作说明）。
14. 冗余展示原则：即使在正文解释中以非代码块形式提及了指令逻辑，也必须在上下文显眼位置提供一份纯净的 ```bash 块供用户快速使用。
15. 换行注释准则：代码块内的注释必须单独成行并置于对应指令的上方，严禁在指令同一行结尾添加注释。
